package net.coobird.thumbnailator.filters.textwriters;

import java.awt.Color;
import java.awt.Graphics2D;

public class MotionTextWriter extends AbstractTextWriter {

	public void write(Graphics2D g, String text, Color color, Color secondaryColor, int x, int y) {
//		for (int i = 0; i < 20; i++) {
//			int w = (g.getFontMetrics()).stringWidth(text);
//			int h = (g.getFontMetrics()).getHeight();
//			doWrite(g, text, new Color(color.getRed() - i * 10, color.getGreen() - i * 10, color.getBlue() - i * 10), (width - w) / 2, shiftSouth(y, 5 * i));
//		}
	}

}
